FROM node:22-alpine

# 設定工作目錄
WORKDIR /app

# 複製依賴檔案並安裝
COPY package*.json ./
RUN npm install

# 暴露 Vite 預設的 5173 port
EXPOSE 5173

# 啟動開發伺服器 (--host 確保外部可存取，--mode development 為開發模式)
CMD ["npm", "run", "dev", "--", "--host"]
